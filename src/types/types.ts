import { Content } from "@prismicio/client";

export interface BriefSliceData {
    title: string;
    description: any;
}

export interface MiniServicesData {
    buttonname: string;
}

export interface MiniServicesSliceData {
    data: MiniServicesData[];
}